query AllPlans($isDiary: Boolean){
    allPlans(isDiary: $isDiary){
      id,
      title,
      description,
      initDate,
      initHour,
      endHour,
      location,
      urlPlanPicture,
      owner {
          username
        }
    }
}